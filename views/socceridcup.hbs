<div class="promo-page gallery-page">

  <!-- ═══ HERO ═══ -->
  <section class="gp-hero">
    <img class="gp-hero__bg" src="{{galeria.banner}}" alt="{{galeria.title}}">
    <div class="gp-hero__dim"></div>
    <div class="gp-hero__grain"></div>

    <a href="/{{lang}}" class="gp-nav-back">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>
    </a>


    <div class="gp-hero__body">
      <div class="gp-hero__logo">
        <img src="/assets/images/iconsoccerid.png" alt="SOCCER iD" height="36">
      </div>
      <h1 class="gp-hero__title">
        <span class="gp-hero__title-main">{{galeria.title}}</span>
        {{#if galeria.year}}<span class="gp-hero__title-year">{{galeria.year}}</span>{{/if}}
      </h1>
      <p class="gp-hero__sub">{{galeria.subtitle}}</p>
      <div class="gp-hero__pills">
        <span class="gp-pill">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M19 4h-1V2h-2v2H8V2H6v2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V10h14v10z"/></svg>
          {{galeria.date}}
        </span>
        <span class="gp-pill">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
          {{galeria.venue}}
        </span>
      </div>
      <a class="gp-hero__wa" href="https://wa.me/17867844303?text={{galeria.ctaMessage}}" target="_blank">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
        {{galeria.ctaText}}
      </a>
      <button class="gp-hero__cta" onclick="document.getElementById('statsSection').scrollIntoView({behavior:'smooth'})" type="button">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M7 13l5 5 5-5M7 6l5 5 5-5"/></svg>
      </button>
    </div>
  </section>

  <!-- ═══ STATS ═══ -->
  <section class="gp-stats" id="statsSection">
    <div class="gp-stats__inner">
      <div class="gp-stats__text">
        <span class="gp-label">{{#if isEs}}EL EVENTO{{else}}THE EVENT{{/if}}</span>
        <h2 class="gp-stats__heading">{{galeria.description}}</h2>
      </div>
      <div class="gp-stats__cards">
        {{#each galeria.stats}}
        <div class="gp-stat-card">
          <span class="gp-stat-card__value">{{this.value}}</span>
          <span class="gp-stat-card__label">{{this.label}}</span>
          <span class="gp-stat-card__sub">{{this.sub}}</span>
        </div>
        {{/each}}
      </div>
    </div>
  </section>

  <!-- ═══ ALCANCE / REACH ═══ -->
  <section class="gp-reach">
    <div class="gp-reach__inner">
      <span class="gp-label">{{#if isEs}}ALCANCE DE LA COPA{{else}}CUP REACH{{/if}}</span>
      <div class="gp-reach__cards">
        {{#each galeria.reach}}
        <div class="gp-reach__card">
          <div class="gp-reach__icon">
            {{#eq this.icon 'stadium'}}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 7v4a9 9 0 0018 0V7"/><path d="M3 7c0-1.1 4.03-2 9-2s9 .9 9 2"/><ellipse cx="12" cy="7" rx="9" ry="2"/></svg>
            {{/eq}}
            {{#eq this.icon 'tv'}}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="7" width="20" height="15" rx="2" ry="2"/><path d="M17 2l-5 5-5-5"/></svg>
            {{/eq}}
            {{#eq this.icon 'social'}}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 01-3.46 0"/></svg>
            {{/eq}}
          </div>
          <span class="gp-reach__value">{{this.value}}</span>
          <span class="gp-reach__label">{{this.label}}</span>
        </div>
        {{/each}}
      </div>
    </div>
  </section>

  <!-- ═══ PREVIOUS EDITIONS ═══ -->
  <section class="gp-editions">
    <div class="gp-editions__inner">
      <span class="gp-label">{{#if isEs}}EDICIONES{{else}}EDITIONS{{/if}}</span>
      <div class="gp-timeline">
        {{#each galeria.highlights}}
        {{#if this.link}}
        <a class="gp-timeline__item gp-timeline__item--link" href="/{{../lang}}/socceridcup/{{this.year}}">
          <span class="gp-timeline__year">{{this.year}}</span>
          <div class="gp-timeline__content">
            <span class="gp-timeline__match">{{this.match}}</span>
            <span class="gp-timeline__city">{{this.city}}</span>
          </div>
          <svg class="gp-timeline__arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M9 18l6-6-6-6"/></svg>
        </a>
        {{else}}
        <div class="gp-timeline__item {{#eq this.tone 'pause'}}gp-timeline__item--pause{{else}}gp-timeline__item--soon{{/eq}}">
          <span class="gp-timeline__year">{{this.year}}</span>
          <div class="gp-timeline__content">
            <span class="gp-timeline__match">{{this.match}}</span>
            <span class="gp-timeline__city">{{this.city}}</span>
          </div>
        </div>
        {{/if}}
        {{/each}}
      </div>
    </div>
  </section>

  <!-- ═══ QUÉ HACEMOS / WHAT WE DO ═══ -->
  <section class="gp-whatwedo">
    <div class="gp-whatwedo__inner">
      <span class="gp-label">{{#if isEs}}QUÉ HACEMOS{{else}}WHAT WE DO{{/if}}</span>
      <div class="gp-whatwedo__grid">
        <div class="gp-whatwedo__card">
          <div class="gp-whatwedo__icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 000 20M12 2a14.5 14.5 0 010 20M2 12h20"/></svg>
          </div>
          <h3>{{#if isEs}}Gestión de Eventos{{else}}Event Management{{/if}}</h3>
          <p>{{#if isEs}}Damos vida a partidos internacionales mediante una planeación y ejecución especializada.{{else}}We bring international soccer matches to life through expert planning and execution.{{/if}}</p>
        </div>
        <div class="gp-whatwedo__card">
          <div class="gp-whatwedo__icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 00-3-3.87M16 3.13a4 4 0 010 7.75"/></svg>
          </div>
          <h3>{{#if isEs}}Clubes{{else}}Clubs{{/if}}</h3>
          <p>{{#if isEs}}Trabajamos con los principales clubes profesionales de Latinoamérica.{{else}}We work with leading professional soccer clubs across Latin America.{{/if}}</p>
        </div>
        <div class="gp-whatwedo__card">
          <div class="gp-whatwedo__icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="7" width="20" height="14" rx="2" ry="2"/><path d="M16 21V5a2 2 0 00-2-2h-4a2 2 0 00-2 2v16"/></svg>
          </div>
          <h3>{{#if isEs}}Estrategia Comercial{{else}}Commercial Strategy{{/if}}</h3>
          <p>{{#if isEs}}Desarrollamos estrategias de boletaje, patrocinios y experiencias premium.{{else}}We develop ticketing, sponsorship, and premium hospitality strategies.{{/if}}</p>
        </div>
        <div class="gp-whatwedo__card">
          <div class="gp-whatwedo__icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="7" width="20" height="15" rx="2" ry="2"/><path d="M17 2l-5 5-5-5"/></svg>
          </div>
          <h3>{{#if isEs}}Medios{{else}}Media{{/if}}</h3>
          <p>{{#if isEs}}Gestionamos la producción de transmisiones y el contenido digital para maximizar el alcance de cada evento.{{else}}We manage broadcast production and digital content to maximize audience reach.{{/if}}</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ GALLERY ═══ -->
  <section class="gp-gallery" id="gallerySection">
    <div class="gp-gallery__inner">
      <div class="gp-gallery__header">
        <div>
          <span class="gp-label">{{#if isEs}}MOMENTOS{{else}}MOMENTS{{/if}}</span>
        </div>
        <span class="gp-gallery__count">{{galeria.imageCount}} {{#if isEs}}fotos{{else}}photos{{/if}}</span>
      </div>
    </div>
    <div class="gp-mosaic">
      {{#each galeria.images}}
      <div class="gp-mosaic__item" onclick="openGalleryLightbox({{@index}})">
        <img src="{{this.src}}" alt="{{this.alt}}" loading="lazy" draggable="false">
        <div class="gp-mosaic__hover">
          <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="11" cy="11" r="8"/><path d="M21 21l-4.35-4.35"/><path d="M11 8v6M8 11h6"/></svg>
        </div>
      </div>
      {{/each}}
    </div>
  </section>

  <!-- ═══ SPONSORS ═══ -->
  <section class="gp-sponsors">
    <div class="gp-sponsors__inner">
      <span class="gp-label">{{#if isEs}}PATROCINADORES{{else}}SPONSORS{{/if}}</span>
      <div class="gp-sponsors__grid">
        <div class="gp-sponsors__item gp-sponsors__item--sq">
          <img src="/assets/images/sponsors/caliente.png" alt="Caliente MX">
        </div>
        <div class="gp-sponsors__item gp-sponsors__item--sq">
          <img src="/assets/images/sponsors/fox-sports.png" alt="Fox Sports">
        </div>
        <div class="gp-sponsors__item gp-sponsors__item--sq">
          <img src="/assets/images/sponsors/voit.png" alt="Voit">
        </div>
        <div class="gp-sponsors__item gp-sponsors__item--sq">
          <img src="/assets/images/sponsors/nuestra-vision.png" alt="Nuestra Visión">
        </div>
        <div class="gp-sponsors__item gp-sponsors__item--wide gp-sponsors__item--bottom">
          <img src="/assets/images/segurosidlogo.png" alt="Seguros iD">
        </div>
      </div>
      <div class="gp-sponsors__marquee">
        <div class="gp-sponsors__track">
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/caliente.png" alt="Caliente MX"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/fox-sports.png" alt="Fox Sports"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/voit.png" alt="Voit"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/nuestra-vision.png" alt="Nuestra Visión"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/caliente.png" alt="Caliente MX"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/fox-sports.png" alt="Fox Sports"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/voit.png" alt="Voit"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/nuestra-vision.png" alt="Nuestra Visión"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/caliente.png" alt="Caliente MX"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/fox-sports.png" alt="Fox Sports"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/voit.png" alt="Voit"></div>
          <div class="gp-sponsors__slide"><img src="/assets/images/sponsors/nuestra-vision.png" alt="Nuestra Visión"></div>
        </div>
        <div class="gp-sponsors__seguros-mobile">
          <img src="/assets/images/segurosidlogo.png" alt="Seguros iD">
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ MEDIA / PRESS ═══ -->
  {{#gt galeria.mediaLinks.length 0}}
  <section class="gp-media">
    <div class="gp-media__inner">
      <span class="gp-label">{{#if isEs}}NOTAS EN MEDIOS{{else}}MEDIA COVERAGE{{/if}}</span>
      <div class="gp-media__list">
        {{#each galeria.mediaLinks}}
        <a class="gp-media__item" href="{{this.url}}" target="_blank" rel="noopener">
          {{#if this.image}}
          <div class="gp-media__thumb">
            <img src="{{this.image}}" alt="" loading="lazy">
          </div>
          {{else}}
          <div class="gp-media__icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M4 22h16a2 2 0 002-2V4a2 2 0 00-2-2H8a2 2 0 00-2 2v16a2 2 0 01-2 2zm0 0a2 2 0 01-2-2v-9c0-1.1.9-2 2-2h2"/><path d="M18 14h-8M18 18h-8M18 10h-8"/></svg>
          </div>
          {{/if}}
          <div class="gp-media__text">
            <span class="gp-media__title">{{this.title}}</span>
            <span class="gp-media__source">{{this.source}}</span>
          </div>
          <svg class="gp-media__arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17L17 7M17 7H7M17 7v10"/></svg>
        </a>
        {{/each}}
      </div>
    </div>
  </section>
  {{/gt}}

  <!-- ═══ CTA ═══ -->
  <section class="gp-cta-section">
    <img class="gp-cta-section__bg" src="/assets/images/gallery/cup2025/5.jpg" alt="">
    <div class="gp-cta-section__dim"></div>
    <div class="gp-cta-section__body">
      <span class="gp-label" style="color:rgba(255,255,255,0.7)">SOCCER iD CUP</span>
      <h2 class="gp-cta-section__title">{{#if isEs}}Sé parte de la experiencia{{else}}Be part of the experience{{/if}}</h2>
      <a class="gp-cta-btn" href="https://wa.me/17867844303?text={{galeria.ctaMessage}}" target="_blank">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
        {{galeria.ctaText}}
      </a>
    </div>
  </section>

  <!-- ═══ FOOTER ═══ -->
  <footer class="gp-footer">
    <a href="/{{lang}}" class="gp-footer__back">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>
      {{#if isEs}}Regresar al inicio{{else}}Back to home{{/if}}
    </a>
    <img src="/assets/images/iconsoccerid.png" alt="SOCCER iD" height="28">
    <span>SOCCER iD &copy; {{year}}</span>
  </footer>

  <!-- ═══ GO TO TOP ═══ -->
  <button class="gp-go-top" id="gpGoTop" onclick="window.scrollTo({top:0,behavior:'smooth'})" type="button" aria-label="Go to top">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M18 15l-6-6-6 6"/></svg>
  </button>
</div>

<!-- ═══ LIGHTBOX ═══ -->
<div class="gl-lightbox" id="glLightbox">
  <div class="gl-lightbox__backdrop" onclick="closeGalleryLightbox()"></div>
  <button class="gl-lightbox__close" onclick="closeGalleryLightbox()" type="button">
    <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 6L6 18M6 6l12 12"/></svg>
  </button>
  <button class="gl-lightbox__nav gl-lightbox__nav--prev" onclick="navigateGallery(-1)" type="button">
    <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 18l-6-6 6-6"/></svg>
  </button>
  <div class="gl-lightbox__img-wrap">
    <img id="glImage" src="" alt="" draggable="false">
  </div>
  <button class="gl-lightbox__nav gl-lightbox__nav--next" onclick="navigateGallery(1)" type="button">
    <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
  </button>
  <div class="gl-lightbox__counter" id="glCounter"></div>
</div>

<style>
  /* ── Reset promo constraints ── */
  .gallery-page.promo-page { max-width: 100%; }
  .gallery-page .promo-back { display: none; }

  /* ── Shared ── */
  .gp-label {
    display: inline-block;
    font-size: 11px;
    font-weight: 800;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: #9b59f0;
    margin-bottom: 10px;
  }
  .gp-section-title {
    font-size: 32px;
    font-weight: 900;
    letter-spacing: -0.5px;
    color: #fff;
  }

  /* ═══════════════════ HERO ═══════════════════ */
  .gp-hero {
    position: relative;
    width: 100%;
    height: 100vh;
    height: 100svh;
    min-height: 560px;
    overflow: hidden;
    display: flex;
    align-items: flex-end;
  }

  .gp-hero__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 20%;
  }

  .gp-hero__dim {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg,
        rgba(10,10,26,0.5) 0%,
        rgba(10,10,26,0) 25%,
        rgba(10,10,26,0.15) 50%,
        rgba(10,10,26,0.85) 78%,
        #0a0a1a 100%);
  }

  .gp-hero__grain {
    position: absolute;
    inset: 0;
    opacity: 0.04;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
    pointer-events: none;
  }

  .gp-nav-back {
    position: absolute;
    top: 16px;
    left: 16px;
    z-index: 20;
    width: 42px;
    height: 42px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0,0,0,0.5);
    -webkit-backdrop-filter: blur(12px);
    backdrop-filter: blur(12px);
    border-radius: 50%;
    border: 1px solid rgba(255,255,255,0.12);
    color: #fff;
    text-decoration: none;
    transition: background 0.2s;
  }
  .gp-nav-back:hover { background: rgba(108,60,224,0.6); }


  .gp-hero__body {
    position: relative;
    z-index: 10;
    width: 100%;
    padding: 0 28px 48px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .gp-hero__logo {
    margin-bottom: 20px;
    opacity: 0.9;
  }

  .gp-hero__title {
    display: flex;
    align-items: baseline;
    gap: 14px;
    margin-bottom: 8px;
    flex-wrap: wrap;
    justify-content: center;
  }
  .gp-hero__title-main {
    font-size: 46px;
    font-weight: 900;
    letter-spacing: 2px;
    line-height: 1;
    text-shadow: 0 4px 40px rgba(0,0,0,0.6);
  }
  .gp-hero__title-year {
    font-size: 46px;
    font-weight: 900;
    letter-spacing: 2px;
    line-height: 1;
    background: linear-gradient(135deg, #9b59f0, #6c3ce0);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: none;
  }

  .gp-hero__sub {
    font-size: 15px;
    color: rgba(255,255,255,0.65);
    font-weight: 500;
    margin-bottom: 20px;
  }

  .gp-hero__pills {
    display: flex;
    gap: 10px;
    margin-bottom: 28px;
  }
  .gp-pill {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 12px;
    font-weight: 700;
    color: rgba(255,255,255,0.8);
    background: rgba(255,255,255,0.08);
    -webkit-backdrop-filter: blur(10px);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255,255,255,0.1);
    padding: 7px 14px;
    border-radius: 20px;
    letter-spacing: 0.5px;
  }
  .gp-pill svg { opacity: 0.6; }

  .gp-hero__wa {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-top: 10px;
    margin-bottom: 16px;
    padding: 7px 16px;
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.15);
    color: rgba(255,255,255,0.7);
    text-decoration: none;
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.6px;
    text-transform: uppercase;
    border-radius: 8px;
    transition: background 0.2s, color 0.2s;
  }
  .gp-hero__wa svg { width: 13px; height: 13px; }
  .gp-hero__wa:active { transform: scale(0.96); }
  .gp-hero__wa:hover { background: rgba(255,255,255,0.15); color: #fff; }

  .gp-hero__cta {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(108,60,224,0.4);
    border: 1px solid rgba(155,89,240,0.4);
    border-radius: 50%;
    color: #fff;
    cursor: pointer;
    animation: gp-bounce 2.5s ease-in-out infinite;
  }
  @keyframes gp-bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(6px); }
  }

  /* ═══════════════════ STATS ═══════════════════ */
  .gp-stats {
    background: #0a0a1a;
    padding: 56px 24px;
  }
  .gp-stats__inner {
    max-width: 960px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 40px;
    align-items: center;
  }
  .gp-stats__heading {
    font-size: 16px;
    font-weight: 400;
    color: rgba(255,255,255,0.9);
    line-height: 1.6;
    text-align: justify;
  }
  .gp-stats__cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
  }
  .gp-stat-card {
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 20px 14px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 4px;
    transition: border-color 0.3s, background 0.3s;
  }
  .gp-stat-card:hover {
    border-color: rgba(108,60,224,0.4);
    background: rgba(108,60,224,0.06);
  }
  .gp-stat-card__value {
    font-size: 36px;
    font-weight: 900;
    background: linear-gradient(135deg, #9b59f0, #c084fc);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
  }
  .gp-stat-card__label {
    font-size: 10px;
    font-weight: 800;
    letter-spacing: 2px;
    color: rgba(255,255,255,0.5);
    margin-top: 4px;
  }
  .gp-stat-card__sub {
    font-size: 11px;
    color: rgba(255,255,255,0.3);
    margin-top: 2px;
  }

  @media (max-width: 700px) {
    .gp-stats__inner { grid-template-columns: 1fr; gap: 28px; }
    .gp-stats__heading { font-size: 16px; text-align: center; }
    .gp-stats { padding: 40px 20px; }
  }

  /* ═══════════════════ WHAT WE DO ═══════════════════ */
  .gp-whatwedo {
    background: linear-gradient(180deg, #0a0a1a 0%, #0e0e28 100%);
    padding: 56px 24px;
  }
  .gp-whatwedo__inner {
    max-width: 960px;
    margin: 0 auto;
  }
  .gp-whatwedo__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-top: 24px;
  }
  .gp-whatwedo__card {
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 28px 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 12px;
    transition: border-color 0.3s, background 0.3s;
  }
  .gp-whatwedo__card:hover {
    border-color: rgba(108,60,224,0.4);
    background: rgba(108,60,224,0.06);
  }
  .gp-whatwedo__icon {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    background: rgba(108,60,224,0.15);
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .gp-whatwedo__icon svg {
    width: 24px;
    height: 24px;
    color: #c084fc;
  }
  .gp-whatwedo__card h3 {
    font-size: 16px;
    font-weight: 800;
    color: #fff;
    margin: 0;
  }
  .gp-whatwedo__card p {
    font-size: 13px;
    color: rgba(255,255,255,0.5);
    line-height: 1.5;
    margin: 0;
    text-align: center;
  }

  @media (max-width: 700px) {
    .gp-whatwedo__grid {
      grid-template-columns: 1fr 1fr;
      gap: 12px;
    }
    .gp-whatwedo__card { padding: 20px 16px; }
  }
  @media (max-width: 440px) {
    .gp-whatwedo__grid { grid-template-columns: 1fr; }
  }

  /* ═══════════════════ REACH ═══════════════════ */
  .gp-reach {
    background: #0e0e28;
    padding: 56px 24px;
  }
  .gp-reach__inner {
    max-width: 960px;
    margin: 0 auto;
  }
  .gp-reach__cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-top: 24px;
  }
  .gp-reach__card {
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 32px 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    transition: border-color 0.3s, background 0.3s;
  }
  .gp-reach__card:hover {
    border-color: rgba(108,60,224,0.4);
    background: rgba(108,60,224,0.06);
  }
  .gp-reach__icon {
    width: 52px;
    height: 52px;
    border-radius: 50%;
    background: rgba(108,60,224,0.15);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 4px;
  }
  .gp-reach__icon svg {
    width: 26px;
    height: 26px;
    color: #c084fc;
  }
  .gp-reach__value {
    font-size: 40px;
    font-weight: 900;
    background: linear-gradient(135deg, #9b59f0, #c084fc);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
  }
  .gp-reach__label {
    font-size: 11px;
    font-weight: 800;
    letter-spacing: 2px;
    color: rgba(255,255,255,0.5);
    text-align: center;
  }

  @media (max-width: 700px) {
    .gp-reach__cards { grid-template-columns: 1fr; gap: 12px; }
    .gp-reach__card { padding: 24px 16px; }
    .gp-reach__value { font-size: 34px; }
  }

  /* ═══════════════════ EDITIONS ═══════════════════ */
  .gp-editions {
    background: #0a0a1a;
    padding: 48px 24px;
  }
  .gp-editions__inner {
    max-width: 960px;
    margin: 0 auto;
  }
  .gp-timeline {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-top: 8px;
  }
  .gp-timeline__item {
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 18px 16px;
    border-bottom: none;
    border-radius: 8px;
  }
  .gp-timeline__year {
    font-size: 14px;
    font-weight: 800;
    color: #9b59f0;
    min-width: 48px;
    letter-spacing: 1px;
  }
  .gp-timeline__content {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .gp-timeline__match {
    font-size: 16px;
    font-weight: 700;
    color: #fff;
  }
  .gp-timeline__city {
    font-size: 13px;
    color: rgba(255,255,255,0.4);
  }
  .gp-timeline__item--soon {
    background: linear-gradient(90deg, rgba(108,60,224,0.12), transparent);
  }
  .gp-timeline__item--soon .gp-timeline__year {
    color: #c084fc;
    font-size: 16px;
  }
  .gp-timeline__item--soon .gp-timeline__match {
    color: #c084fc;
    font-style: italic;
  }
  .gp-timeline__item--pause {
    background: linear-gradient(90deg, rgba(234,179,8,0.08), transparent);
  }
  .gp-timeline__item--pause .gp-timeline__year {
    color: #eab308;
    font-size: 16px;
  }
  .gp-timeline__item--pause .gp-timeline__match {
    color: #eab308;
    font-style: italic;
  }
  .gp-timeline__item--pause .gp-timeline__city {
    color: rgba(234,179,8,0.5);
  }
  .gp-timeline__item--link {
    text-decoration: none;
    cursor: pointer;
    transition: background 0.2s;
  }
  .gp-timeline__item--link:hover {
    background: rgba(108,60,224,0.08);
  }
  .gp-timeline__arrow {
    color: rgba(255,255,255,0.2);
    flex-shrink: 0;
    margin-left: auto;
    transition: color 0.2s;
  }
  .gp-timeline__item--link:hover .gp-timeline__arrow {
    color: #9b59f0;
  }

  /* ═══════════════════ GALLERY ═══════════════════ */
  .gp-gallery {
    background: linear-gradient(180deg, #0a0a1a 0%, #0e0e28 100%);
    padding: 48px 0 80px;
  }
  .gp-gallery__inner {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 24px 20px;
  }
  .gp-gallery__header {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
  }
  .gp-gallery__count {
    font-size: 13px;
    font-weight: 700;
    color: rgba(255,255,255,0.3);
    background: rgba(255,255,255,0.05);
    padding: 5px 14px;
    border-radius: 12px;
    margin-bottom: 4px;
  }

  .gp-mosaic {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-auto-rows: 260px;
    gap: 4px;
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 4px;
  }

  .gp-mosaic__item:nth-child(1) { grid-column: span 2; grid-row: span 2; }
  .gp-mosaic__item:nth-child(6) { grid-column: span 2; }

  .gp-mosaic__item {
    position: relative;
    overflow: hidden;
    border-radius: 8px;
    cursor: pointer;
    background: rgba(255,255,255,0.03);
  }
  .gp-mosaic__item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.5s ease;
  }
  .gp-mosaic__hover {
    position: absolute;
    inset: 0;
    background: transparent;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.35s;
  }
  .gp-mosaic__hover svg {
    color: #fff;
    opacity: 0;
    transform: scale(0.7);
    transition: all 0.35s;
    filter: drop-shadow(0 2px 10px rgba(0,0,0,0.5));
  }
  .gp-mosaic__item:hover img { transform: scale(1.1); }
  .gp-mosaic__item:hover .gp-mosaic__hover {
    background: linear-gradient(135deg, rgba(108,60,224,0.4), rgba(155,89,240,0.25));
  }
  .gp-mosaic__item:hover .gp-mosaic__hover svg { opacity: 1; transform: scale(1); }

  @media (max-width: 700px) {
    .gp-mosaic {
      grid-template-columns: repeat(2, 1fr);
      grid-auto-rows: 170px;
      gap: 3px;
      padding: 0 3px;
    }
    .gp-mosaic__item:nth-child(1) { grid-column: span 2; grid-row: span 1; }
    .gp-mosaic__item:nth-child(6) { grid-column: span 1; }
    .gp-mosaic__item:nth-child(8) { grid-column: span 2; }
    .gp-mosaic__hover svg { opacity: 0.5; transform: scale(1); }
    .gp-hero__sub { font-size: 14px; text-align: center; }
    .gp-hero__title-main { font-size: 30px; }
    .gp-hero__title-year { font-size: 30px; }
    .gp-section-title { font-size: 24px; }
    .gp-gallery__inner { padding: 0 16px 16px; }
  }

  /* ═══════════════════ SPONSORS ═══════════════════ */
  .gp-sponsors {
    background: #0e0e28;
    padding: 60px 24px;
    border-top: 1px solid rgba(108,60,224,0.15);
  }
  .gp-sponsors__inner {
    max-width: 960px;
    margin: 0 auto;
  }
  .gp-sponsors__grid {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 48px;
    margin-top: 32px;
    flex-wrap: wrap;
  }
  .gp-sponsors__item {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 16px 28px;
    border-radius: 12px;
    transition: transform 0.3s;
  }
  .gp-sponsors__item:hover {
    transform: scale(1.06);
  }
  .gp-sponsors__item img {
    object-fit: contain;
    opacity: 0.85;
    transition: opacity 0.3s;
  }
  .gp-sponsors__item:hover img {
    opacity: 1;
  }
  .gp-sponsors__item--wide img {
    width: 230px;
    height: auto;
  }
  .gp-sponsors__item--sq img {
    height: 76px;
    width: auto;
  }
  .gp-sponsors__item--bottom {
    width: 100%;
    margin-top: 8px;
  }
  .gp-sponsors__marquee {
    display: none;
  }
  .gp-sponsors__seguros-mobile {
    display: none;
  }
  @media (max-width: 700px) {
    .gp-sponsors__grid { display: none; }
    .gp-sponsors__marquee {
      display: block;
      overflow: hidden;
      margin-top: 32px;
      -webkit-mask-image: linear-gradient(90deg, transparent, #000 6%, #000 94%, transparent);
      mask-image: linear-gradient(90deg, transparent, #000 6%, #000 94%, transparent);
    }
    .gp-sponsors__track {
      display: flex;
      align-items: center;
      gap: 0;
      width: max-content;
      animation: sponsorScroll 30s linear infinite;
    }
    .gp-sponsors__slide {
      flex-shrink: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      width: 160px;
      height: 76px;
    }
    .gp-sponsors__slide img {
      max-width: 140px;
      max-height: 68px;
      object-fit: contain;
      opacity: 0.9;
    }
    .gp-sponsors__seguros-mobile {
      display: flex;
      align-items: center;
      justify-content: center;
      margin-top: 28px;
    }
    .gp-sponsors__seguros-mobile img {
      width: 200px;
      opacity: 0.85;
      object-fit: contain;
    }
  }
  @keyframes sponsorScroll {
    0% { transform: translateX(0); }
    100% { transform: translateX(-33.333%); }
  }

  /* ═══════════════════ MEDIA / PRESS ═══════════════════ */
  .gp-media { background: #0a0a1a; padding: 48px 24px; }
  .gp-media__inner { max-width: 960px; margin: 0 auto; }
  .gp-media__list { display: flex; flex-direction: column; gap: 8px; margin-top: 20px; }
  .gp-media__item { display: flex; align-items: center; gap: 16px; padding: 16px 20px; background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.06); border-radius: 12px; text-decoration: none; transition: border-color 0.3s, background 0.3s; }
  .gp-media__item:hover { border-color: rgba(108,60,224,0.4); background: rgba(108,60,224,0.06); }
  .gp-media__thumb { width: 72px; height: 52px; border-radius: 8px; overflow: hidden; flex-shrink: 0; }
  .gp-media__thumb img { width: 100%; height: 100%; object-fit: cover; }
  .gp-media__icon { width: 40px; height: 40px; border-radius: 10px; background: rgba(108,60,224,0.15); display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
  .gp-media__icon svg { color: #c084fc; }
  .gp-media__text { flex: 1; display: flex; flex-direction: column; gap: 2px; }
  .gp-media__title { font-size: 14px; font-weight: 700; color: #fff; }
  .gp-media__source { font-size: 12px; color: rgba(255,255,255,0.4); }
  .gp-media__arrow { color: rgba(255,255,255,0.3); flex-shrink: 0; transition: color 0.2s; }
  .gp-media__item:hover .gp-media__arrow { color: #c084fc; }

  /* ═══════════════════ CTA SECTION ═══════════════════ */
  .gp-cta-section {
    position: relative;
    padding: 80px 24px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .gp-cta-section__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 40%;
  }
  .gp-cta-section__dim {
    position: absolute;
    inset: 0;
    background: rgba(10,10,26,0.6);
    -webkit-backdrop-filter: blur(2px);
    backdrop-filter: blur(2px);
  }
  .gp-cta-section__body {
    position: relative;
    z-index: 1;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
  }
  .gp-cta-section__title {
    font-size: 26px;
    font-weight: 700;
    letter-spacing: -0.3px;
    margin-bottom: 24px;
  }
  .gp-cta-btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 16px 36px;
    background: linear-gradient(135deg, #6c3ce0, #9b59f0);
    color: #fff;
    text-decoration: none;
    font-size: 15px;
    font-weight: 800;
    letter-spacing: 1px;
    text-transform: uppercase;
    border-radius: 14px;
    box-shadow: 0 8px 32px rgba(108,60,224,0.5);
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .gp-cta-btn:active { transform: scale(0.96); }
  .gp-cta-btn:hover { box-shadow: 0 12px 48px rgba(108,60,224,0.65); }

  @media (max-width: 700px) {
    .gp-cta-section { padding: 60px 20px; }
    .gp-cta-section__title { font-size: 22px; }
  }

  /* ═══════════════════ FOOTER ═══════════════════ */
  .gp-footer {
    background: #0a0a1a;
    padding: 24px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }
  .gp-footer__back {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: rgba(255,255,255,0.5);
    text-decoration: none;
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.5px;
    padding: 10px 24px;
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 10px;
    margin-bottom: 16px;
    transition: color 0.2s, border-color 0.2s;
  }
  .gp-footer__back:hover {
    color: rgba(255,255,255,0.8);
    border-color: rgba(255,255,255,0.35);
  }
  .gp-footer span {
    font-size: 11px;
    color: rgba(255,255,255,0.25);
  }
  .gp-footer img { opacity: 0.5; }

  /* ═══════════════════ GO TO TOP ═══════════════════ */
  .gp-go-top {
    position: fixed;
    bottom: 28px;
    right: 28px;
    z-index: 900;
    width: 46px;
    height: 46px;
    border: none;
    border-radius: 50%;
    background: rgba(108,60,224,0.85);
    color: #fff;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 16px rgba(0,0,0,0.4);
    opacity: 0;
    visibility: hidden;
    transform: translateY(12px);
    transition: opacity 0.3s, visibility 0.3s, transform 0.3s;
    -webkit-backdrop-filter: blur(8px);
    backdrop-filter: blur(8px);
  }
  .gp-go-top.visible {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }
  .gp-go-top:active { transform: scale(0.92); }

  /* ═══════════════════ LIGHTBOX ═══════════════════ */
  .gl-lightbox {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 10000;
    align-items: center;
    justify-content: center;
  }
  .gl-lightbox.active { display: flex; }

  .gl-lightbox__backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,0.96);
  }

  .gl-lightbox__close {
    position: absolute;
    top: 16px;
    right: 16px;
    z-index: 2;
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.1);
    color: #fff;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s;
  }
  .gl-lightbox__close:hover { background: rgba(255,255,255,0.15); }

  .gl-lightbox__img-wrap {
    position: relative;
    z-index: 1;
    max-width: 92vw;
    max-height: 85vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .gl-lightbox__img-wrap img {
    max-width: 92vw;
    max-height: 85vh;
    object-fit: contain;
    border-radius: 6px;
    user-select: none;
    box-shadow: 0 8px 60px rgba(0,0,0,0.6);
  }

  .gl-lightbox__nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 2;
    background: rgba(255,255,255,0.06);
    border: 1px solid rgba(255,255,255,0.1);
    color: #fff;
    width: 52px;
    height: 52px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
  }
  .gl-lightbox__nav:hover { background: rgba(108,60,224,0.5); border-color: rgba(108,60,224,0.5); }
  .gl-lightbox__nav--prev { left: 16px; }
  .gl-lightbox__nav--next { right: 16px; }

  .gl-lightbox__counter {
    position: absolute;
    bottom: 24px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    background: rgba(0,0,0,0.5);
    -webkit-backdrop-filter: blur(10px);
    backdrop-filter: blur(10px);
    color: #fff;
    font-size: 14px;
    font-weight: 700;
    padding: 8px 20px;
    border-radius: 24px;
    letter-spacing: 2px;
    border: 1px solid rgba(255,255,255,0.06);
  }

  @media (max-width: 600px) {
    .gl-lightbox__nav { width: 42px; height: 42px; }
    .gl-lightbox__nav--prev { left: 8px; }
    .gl-lightbox__nav--next { right: 8px; }
    .gl-lightbox__img-wrap img { max-width: 96vw; border-radius: 4px; }
  }
</style>

<script>
(function() {
  var images = [{{#each galeria.images}}{ src: "{{this.src}}", alt: "{{this.alt}}" }{{#unless @last}},{{/unless}}{{/each}}];
  var currentIdx = 0;
  var lightbox = document.getElementById('glLightbox');
  var imgEl = document.getElementById('glImage');
  var counterEl = document.getElementById('glCounter');
  var startX = 0;

  window.openGalleryLightbox = function(idx) {
    currentIdx = idx;
    showImage();
    lightbox.classList.add('active');
    document.body.style.overflow = 'hidden';
  };
  window.closeGalleryLightbox = function() {
    lightbox.classList.remove('active');
    document.body.style.overflow = '';
  };
  window.navigateGallery = function(dir) {
    currentIdx = (currentIdx + dir + images.length) % images.length;
    showImage();
  };
  function showImage() {
    imgEl.src = images[currentIdx].src;
    imgEl.alt = images[currentIdx].alt;
    counterEl.textContent = (currentIdx + 1) + ' / ' + images.length;
  }
  document.addEventListener('keydown', function(e) {
    if (!lightbox.classList.contains('active')) return;
    if (e.key === 'Escape') closeGalleryLightbox();
    if (e.key === 'ArrowLeft') navigateGallery(-1);
    if (e.key === 'ArrowRight') navigateGallery(1);
  });
  lightbox.addEventListener('touchstart', function(e) { startX = e.touches[0].clientX; }, { passive: true });
  lightbox.addEventListener('touchend', function(e) {
    var diff = e.changedTouches[0].clientX - startX;
    if (Math.abs(diff) > 50) navigateGallery(diff > 0 ? -1 : 1);
  }, { passive: true });
})();

(function() {
  var btn = document.getElementById('gpGoTop');
  if (!btn) return;
  window.addEventListener('scroll', function() {
    btn.classList.toggle('visible', window.scrollY > 400);
  }, { passive: true });
})();
</script>
